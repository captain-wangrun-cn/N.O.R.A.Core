{# 图片标签提取相关模板 #}

{% block image_tags_prompt %}
📎 本次消息附带了以下图片：
{{ image_id_lines }}

请在回复最末尾，为每张图片附上【关键词标签列表】和【图片中的文字】（用于图片记忆检索），格式如下：

[IMAGE_TAGS:img_xxxxxxxx]
标签1, 标签2, 标签3, 标签4 ...（仅关键词，不要写成长句描述，使用英文逗号分隔）
[/IMAGE_TAGS]
[IMAGE_OCR:img_xxxxxxxx]
图片中出现的所有可见文字内容，原样输出（保留换行和排版）
[/IMAGE_OCR]

ID 规则（非常重要）：
1) `IMAGE_TAGS` 和 `IMAGE_OCR` 中的 `img_xxx` 必须使用上方“图片 ID”列表里给出的**原始 ID**；
2) 严禁使用示例占位符（如 `img_xxxxxxxx`）或自行编造 ID；
3) 每个给定图片 ID 都必须且只能出现一组对应的 `[IMAGE_TAGS:ID]` 与 `[IMAGE_OCR:ID]`。
4) **无论模型是否识别成功，都必须输出以上两个区块**。如果无法识别标签，用占位词填充（如：`未识别`、`模糊`、`未知`）；如果没有任何文字，遵循 OCR 规则 3。

标签要求：
1) 每个标签 2-8 个字，优先名词/短词组；
2) 总数 8-20 个；
3) 覆盖主体、场景、物体、颜色、风格、动作、情绪；
4) 标签必须基于图像客观事实，**禁止**臆测、编造或引用图片外的信息，不要受其他图片或文本描述影响；
5) 禁止输出完整句子、解释性文字、编号或 Markdown。

OCR 文字要求：
1) 如果图片中**有任何可见文字**（包括标题、正文、标签、水印、按钮文字、代码、聊天记录截图等），请**全部原样输出**到 [IMAGE_OCR] 块中；
2) 保留原文的换行与排版结构；
3) 如果图片中**完全没有文字**，则输出 [IMAGE_OCR:img_xxx]（无文字）[/IMAGE_OCR]；
4) 不要添加任何解释、翻译或编号。

这些标签和 OCR 文字只用于后台存储，不会展示给用户。
{% endblock %}
