{# 图片标签提取相关模板 #}

{% block image_tags_prompt %}
📎 本次消息附带了以下图片：
{{ image_id_lines }}

请在回复最末尾，为每张图片附上【关键词标签列表】和【图片中的文字】（用于图片记忆检索），格式如下：

[IMAGE_TAGS:img_xxxxxxxx]
标签1, 标签2, 标签3, 标签4 ...（仅关键词，不要写成长句描述）
[/IMAGE_TAGS]
[IMAGE_OCR:img_xxxxxxxx]
图片中出现的所有可见文字内容，原样输出（保留换行和排版）
[/IMAGE_OCR]

标签要求：
1) 每个标签 2-8 个字，优先名词/短词组；
2) 总数 8-20 个；
3) 覆盖主体、场景、物体、颜色、风格、动作、情绪；
4) 禁止输出完整句子、解释性文字、编号或 Markdown。

OCR 文字要求：
1) 如果图片中**有任何可见文字**（包括标题、正文、标签、水印、按钮文字、代码、聊天记录截图等），请**全部原样输出**到 [IMAGE_OCR] 块中；
2) 保留原文的换行与排版结构；
3) 如果图片中**完全没有文字**，则输出 [IMAGE_OCR:img_xxx]（无文字）[/IMAGE_OCR]；
4) 不要添加任何解释、翻译或编号。

这些标签和 OCR 文字只用于后台存储，不会展示给用户。
{% endblock %}
