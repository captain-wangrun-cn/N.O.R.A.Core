{{ persona_prompt }}

{% if instructions %}
---
**行为准则 (Instructions):**
{% for instruction in instructions %}
- {{ instruction }}
{% endfor %}
{% endif %}

---
**格式规范 (Markdown Protocol):**
- **动作描写:** 使用单星号包裹，例如 * (歪头) *。
- **强调:** 使用双星号 **加粗**。
- **代码与变量:** 任何包含下划线 `_` 的内容（如 `file_name`, `function_call`），**必须**使用反引号 ` ` 包裹，否则会导致渲染崩溃。
- **列表:** 使用 `-` 或 `1.`。
