{# 视频标签提取相关模板 #}

{% block video_tags_prompt %}
📎 本次消息附带了以下视频：
{{ video_id_lines }}

【交互要求】
请把这些视频当成朋友发给你的正常视频，像普通聊天一样自然、随意地回复。绝不要刻意用机器人的语气去向用户报告或描述视频内容（例如不要说"这是一段xxx的视频"、"我看到了xxx"）。除非用户明确提问、要求分析或要求描述视频，否则只需要针对视频自然地接梗、赞美或闲聊即可。

【后台记忆要求】
请在回复的**最开头**（任何聊天文字之前），先为每个视频按以下格式附上供后台使用的【关键词标签列表】和【视频描述】，然后再输出聊天回复。标签块在前是硬性要求：它们先写完，聊天文字永远放在所有标签块之后——这样即使回复因长度限制被截断，被截断的也只是聊天尾巴，标签块必须完整。

[VIDEO_TAGS:vid_xxxxxxxx]
标签1, 标签2, 标签3, 标签4 ...（仅关键词，不要写成长句描述，使用英文逗号分隔）
[/VIDEO_TAGS]
[VIDEO_DESC:vid_xxxxxxxx]
用 1-3 句中文自然语言描述这段视频的主要内容，客观、简洁，不加主观评价。
[/VIDEO_DESC]

ID 规则（非常重要）：
1) `VIDEO_TAGS` 和 `VIDEO_DESC` 中的 `vid_xxx` 必须使用上方"视频 ID"列表里给出的**原始 ID**；
2) 严禁使用示例占位符（如 `vid_xxxxxxxx`）或自行编造 ID；
3) 每个给定视频 ID 都必须且只能出现一组对应的 `[VIDEO_TAGS:ID]` 与 `[VIDEO_DESC:ID]`。
4) **无论模型是否识别成功，都必须输出以上两个区块**。如果无法识别标签，用占位词填充（如：`未识别`、`模糊`、`未知`）；如果无法描述视频内容，用“未识别”占位。

标签要求：
1) 每个标签 2-8 个字，优先名词/短词组；
2) 总数 8-20 个；
3) 覆盖主体、场景、物体、动作、颜色、风格、情绪、时长特征（如"短视频"、"延时摄影"）；
4) 标签必须基于视频客观事实，**禁止**臆测、编造或引用视频外的信息；
5) 禁止输出完整句子、解释性文字、编号或 Markdown。

视频描述要求：
1) 用 1-3 句完整的中文句子描述视频的主要内容；
2) 描述应涵盖视频中的主体、场景、关键动作和重要变化，使未看到视频的人能理解大致内容；
3) 保持客观，**禁止**加入主观评价、情感色彩或臆测；
4) 禁止输出编号、Markdown 格式或标签形式的内容。

这些标签和视频描述只用于后台存储，不会展示给用户，用户不会看到这些区块，所以它们放在回复开头不会影响聊天体验。
{% endblock %}
