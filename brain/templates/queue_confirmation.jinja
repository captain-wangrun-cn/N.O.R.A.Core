{# 忙碌时后脑入队二次确认模板 #}

{% block ask_system %}{% if soul %}{{ soul }}

---
{% endif %}{% if identity_context %}{{ identity_context }}

---
{% endif %}你是前脑即时回复助手。当前后脑正在忙碌，用户提出了一个需要后脑处理的新请求。
你的任务：
1) 明确告知“后脑正在处理中”；
2) 展示当前队列情况（如果有）；
3) 明确询问用户是否确认把新请求加入队列；
4) 语气自然口语化，不使用 Markdown。
输出仅为给用户看的聊天文本，如果有多个段落，请使用 [SPLIT] 或 [SPLIT:秒数] 分隔以便模拟人类打字的停顿。{% endblock %}

{% block ask_user %}请基于以下信息生成一条“二次确认入队”消息：

【待入队的新请求】
{{ pending_request_text }}

{% if progress_summary %}【后脑当前进度】
{{ progress_summary }}

{% endif %}{% if queue_summary %}【当前队列详情】
{{ queue_summary }}

{% else %}【当前队列详情】
（暂无排队任务）

{% endif %}要求：
- 先简短说明你正在处理中的任务状态；
- 再说明当前队列情况；
- 最后明确询问：是否确认把“待入队的新请求”加入队列；
- 用户可回答“确认/取消”或同义表达。{% endblock %}

{% block ask_fallback %}我这边后脑还在处理中，当前队列如下：
{{ queue_summary if queue_summary else '（暂无排队任务）' }}
要不要把你刚刚这个新请求也加入排队？你回复“确认”我就加入，回复“取消”我就先不排。{% endblock %}

{% block detect_system %}{% if soul %}{{ soul }}

---
{% endif %}{% if identity_context %}{{ identity_context }}

---
{% endif %}你是确认意图解析器。你只做一件事：判断用户是否确认将请求加入后脑队列，还是拒绝，或者无视了排队询问开始了新话题。
你必须严格输出一行：
DECISION:<confirm|cancel|unclear|ignore> | REPLY:<给用户的一句话>
不要输出任何其它内容。{% endblock %}

{% block detect_user %}请判断用户这句话对“是否加入队列”的意图。

【原待入队请求】{{ pending_request_text }}
【队列详情】{{ queue_summary if queue_summary else '（暂无排队任务）' }}
【用户回复】{{ user_text }}

判定规则：
- 明确同意加入排队 => confirm
- 明确拒绝/暂不排队 => cancel
- 发送了与排队无关的新指令、或者重复发送了其它内容（无视了排队确认） => ignore
- 含糊不清且还在回答排队问题 => unclear

输出格式必须是：
DECISION:<confirm|cancel|unclear|ignore> | REPLY:<一句自然口语回复>
或者如果完全静音不进行回复，只静默操作，可在 REPLY 后追加 `[NO_REPLY]`。

其中 REPLY 要求：
- confirm: 简短确认已加入排队（1句话即可）。
- cancel: 简短确认暂不排队（如“好的，已取消排队”），态度自然，绝对不要给自己加戏或过度解释。
- ignore: 必须留空（即不用生成任何回复文字），系统会自动放行。
- unclear: 简明提醒用户只需回复“确认”或“取消”。{% endblock %}

{% block auto_decide_system %}{% if soul %}{{ soul }}

---
{% endif %}{% if identity_context %}{{ identity_context }}

---
{% endif %}你是“后脑忙碌时是否入队”的内部决策器（仅系统内部使用，不对用户发消息）。
你必须严格输出一行：
DECISION:<enqueue|skip>
不要输出任何其它内容。{% endblock %}

{% block auto_decide_user %}请根据以下信息判断：这个请求是否应加入后脑队列。

【用户新请求】
{{ pending_request_text }}

{% if front_reply %}【前脑已回复用户】
{{ front_reply }}

{% endif %}{% if task_instruction %}【前脑任务指示】
{{ task_instruction }}

{% endif %}{% if progress_summary %}【后脑当前进度】
{{ progress_summary }}

{% endif %}【当前队列】
{{ queue_summary if queue_summary else '（暂无排队任务）' }}

判定原则：
- 用户请求明确需要工具/后脑处理，且不是 stop/change/cancel/清空这类中断意图 -> enqueue
- 该消息本质是闲聊、或与任务无关，不值得排队 -> skip
- 不确定时默认 enqueue（避免漏掉用户请求）

仅输出：DECISION:<enqueue|skip>{% endblock %}

{% block confirm_fallback %}收到，我这就把这个新请求加入队列，等当前任务完成后按顺序处理。{% endblock %}
{% block cancel_fallback %}好的，那我先不把这个请求加入队列。你想排队时随时告诉我。{% endblock %}
{% block unclear_fallback %}我这边需要你确认一下：要加入队列请回复“确认”，不加入请回复“取消”。{% endblock %}
