# 词库目录说明

- `always/`：常加载词库（启动即加载）
- `lazy/`：懒加载词库（首次命中时按文件加载）
- 文件格式：每行 `词: 含义`，支持 `#` 注释
- 支持自定义提示：`@prompt: 这份词库的用途说明`

示例：

```text
@prompt: 这份词库用于解释常见网络缩写，语气尽量口语化
YYDS: 永远的神，表示非常厉害
KPI: 关键绩效指标（Key Performance Indicator）
```
